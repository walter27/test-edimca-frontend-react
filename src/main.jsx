import React from 'react';
import ReactDOM from 'react-dom/client';
import { AppEdimca } from './AppEdimca';
import './style.css';
AppEdimca;
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppEdimca />
  </React.StrictMode>
);
